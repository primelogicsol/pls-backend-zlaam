{
  "name": "fiverr-clone",
  "version": "1.0.0",
  "description": "A freenlance plate form for freelancer just like fiverr",
  "main": "index.js",
  "type": "module",
  "author": "zlaam",
  "license": "MIT",
  "scripts": {
    "build": "rimraf -rf ./dist && deno fmt && deno lint && npx tsc",
    "dev": " yarn build  && deno -A --check --watch --env-file .env ./server.ts",
    "start": " yarn build && node --env-file .env ./dist/server.js"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/express": "^5.0.0",
    "prisma": "^5.21.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@prisma/client": "^5.21.1",
    "@types/node": "^22.8.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.21.1"
  }
}
